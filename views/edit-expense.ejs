<!DOCTYPE html>
<html>
    <meta lang="en">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Expense Tracker App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
   
</html>
<body style="background-color: yellow;">
    <div class="container">
        <div>
            <h1 class="text-center">Expense Traker</h1>
        </div>
        <br>
        <br>
        <form action="/updatedexpenses" method="post">
        <div  >
            <label for='catagory-select'>Catagory:</label>
            <select id='catagory-select' class='form-select' name="category">
                <option value="Movie" <% if (editing && expense.category === "Movie") { %>selected<% } %>>Movie</option>
                <option value="Travel" <% if (editing && expense.category === "Travel") { %>selected<% } %>>Travel</option>
                <option value="Food" <% if (editing && expense.category === "Food") { %>selected<% } %>>Food</option>
                <option value="Personal Care" <% if (editing && expense.category === "Personal Care") { %>selected<% } %>>Personal Care</option>
                <option value="Savings" <% if (editing && expense.category === "Savings") { %>selected<% } %>>Savings</option>
              </select><br><br>
    
            <label for ='amount-input' >Amount  :</label>
            <input type="number" id='amount-input' min="0" placeholder="0" class="form-control" name="amount" value="<% if (editing) { %><%= expense.amount %><% } %>" >
       <br><br> <label for="date-input" > Date:</label>
            <input type="date" id="date-input" class="form-control" name="date" value="<%= editing ? expense.date.toISOString().substr(0, 10) : '' %>">
            <input type="hidden" value="<%= expense.id %>" name="id">
        </div>   
         <br><button type="submit" id="add-btn" class="btn btn-primary ">Save</button>
            </form>
            

    </div>
    
</body>